# Custom health check message
PING_MESSAGE=pong

# Skip login/signup (local dev / preview only)
# AUTH_DISABLED=true

# Apollo.io API key — fallback for on-demand contact/company enrichment when
# no vault secret is set for the active org (see server/helpers/apollo-client.ts)
# APOLLO_API_KEY=
