diff --git a/dist/client/resources/McpIntegrationDialog.js b/dist/client/resources/McpIntegrationDialog.js
index 7faddbbe7c0ab226677d4e161211f6c40e2e9287..6cf2187d33e4665edeafb326538cc51c740247ce 100644
--- a/dist/client/resources/McpIntegrationDialog.js
+++ b/dist/client/resources/McpIntegrationDialog.js
@@ -154,7 +154,15 @@ export function McpIntegrationDialog({ open, onOpenChange, initialIntegrationId
             name: args.name,
             url: args.url,
             description: args.description,
-            scope: safeDefaultScope,
+            // Fix: this used to hardcode `safeDefaultScope` (the scope
+            // computed once when the dialog opened), completely ignoring the
+            // live Personal/Organization toggle the user can click via
+            // renderScopeSelector()'s setScope() calls — meaning an OAuth
+            // connection always saved to whatever scope was the default on
+            // open, regardless of what the user actually selected. Use the
+            // live `scope` state instead, so the toggle genuinely controls
+            // where an OAuth-based connection is saved.
+            scope: scope,
             returnUrl,
         })));
     };
